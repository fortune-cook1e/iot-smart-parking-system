import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  /* config options here */
  // reactStrictMode: false, // Disabled to avoid useEffect double invocation issues
};

export default nextConfig;
