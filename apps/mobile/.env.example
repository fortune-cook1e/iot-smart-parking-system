# API Configuration
# For iOS Simulator or real device on same network, use your local IP
# For Android Emulator, use 10.0.2.2 (this is handled automatically in code)
# EXPO_PUBLIC_API_URL=http://localhost:3000

# Example for real device on same network:
# EXPO_PUBLIC_API_URL=http://192.168.1.100:3000
